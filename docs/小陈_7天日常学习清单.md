# 小陈 7 天日常学习清单

> 适用时间：本周 7 天
> 目标：项目推进不断线，八股开始成体系，算法低强度持续，Linux / Docker 快速上手到可实操，补一点前端三件套基础。
> 参考你的 GitHub `README.md` 已避开这些已做题：`1, 2, 4, 5, 19, 21, 26, 160, 175, 176, 206, 209, 876`

## 每日建议时间分配

- 项目推进：4 小时
- 八股：1.5 小时
- LeetCode：1 小时
- Linux / Docker：1 小时
- 前端基础：0.5 小时

---

## Day 1

### 项目推进

- 今日目标：
- 今日实际完成：
- 遇到的问题：
- 明日衔接点：

### 八股模块

**主题：Java 基础高频 1**

- 面向对象三大特性
- 重载和重写的区别
- `==` 和 `equals()` 的区别
- `String`、`StringBuilder`、`StringBuffer` 的区别
- 接口和抽象类的区别
- Java 异常体系

**掌握标准**

- 每个点能用自己的话讲 1 分钟
- 至少能举出 1 个代码或项目里的例子

### LeetCode

- `217. Contains Duplicate`
- `27. Remove Element`

**掌握标准**

- 会讲哈希表和双指针的基本思路
- 能独立写出代码并说明时间复杂度

### Linux / Docker

**主题：服务器连接与基础运维起步**

- 会使用 `ssh`
- 会使用 `scp`
- 会查看服务器目录结构：`ls -lah`、`tree`
- 会使用 `vim` 或 `nano` 进行基础编辑
- 会查看系统信息：`uname -a`、`hostname`、`whoami`
- 会查看磁盘和内存：`df -h`、`free -h`

**掌握标准**

- 能独立连上腾讯云服务器
- 能在服务器上创建目录、上传文件、编辑文件
- 能看懂服务器的基础资源使用情况

### 前端补充

**主题：HTML 基础**

- HTML 文档基本结构
- 常见标签：`div`、`span`、`a`、`img`、`table`、`form`、`input`、`button`
- 表单提交的基本概念

---

## Day 2

### 项目推进

- 今日目标：
- 今日实际完成：
- 遇到的问题：
- 明日衔接点：

### 八股模块

**主题：Java 集合**

- `ArrayList` 和 `LinkedList` 的区别
- `HashMap` 的基本原理
- `HashMap` 和 `Hashtable` 的区别
- `List`、`Set`、`Map` 的区别
- 为什么重写 `equals()` 通常要重写 `hashCode()`
- 常见集合使用场景

### LeetCode

- `35. Search Insert Position`
- `977. Squares of a Sorted Array`

### Linux / Docker

**主题：日志与查找能力**

- 熟练使用 `grep`
- 熟练使用 `find`
- 会用 `tail -f`
- 会用 `head`、`less`
- 会用 `wc`
- 会用 `history`
- 会用管道：`|` 的基础组合

**掌握标准**

- 能在服务器日志中搜索关键错误
- 能定位某个文件或某类文件
- 能用一条命令完成简单筛选

### 前端补充

**主题：CSS 基础**

- 选择器
- 盒模型
- `display`
- `margin` / `padding`
- `position`

---

## Day 3

### 项目推进

- 今日目标：
- 今日实际完成：
- 遇到的问题：
- 明日衔接点：

### 八股模块

**主题：MySQL 基础**

- 主键、外键、唯一约束、非空约束
- SQL 执行顺序
- `where` 和 `having` 的区别
- 内连接和左连接的区别
- `group by` 的作用
- 索引是什么
- 为什么索引能提高查询效率

### LeetCode

- `242. Valid Anagram`
- `349. Intersection of Two Arrays`

### Linux / Docker

**主题：权限、用户与压缩**

- 文件权限位的含义
- `chmod`
- `chown`
- `tar`
- `zip` / `unzip`
- `sudo`
- 用户与组的基本概念

**掌握标准**

- 能修改文件权限和归属
- 能完成压缩、解压和文件打包
- 理解为什么有些操作必须提权

### 前端补充

**主题：CSS 布局**

- `flex`
- 常见居中方式
- 响应式布局的基本概念
- 浏览器开发者工具查看元素样式

---

## Day 4

### 项目推进

- 今日目标：
- 今日实际完成：
- 遇到的问题：
- 明日衔接点：

### 八股模块

**主题：JDBC / MyBatis / 事务**

- JDBC 的执行流程
- MyBatis 相对 JDBC 的优势
- `#{}` 和 `${}` 的区别
- MyBatis 的 Mapper 是干什么的
- 事务的四大特性 ACID
- 事务隔离级别的基本概念

### LeetCode

- `20. Valid Parentheses`
- `232. Implement Queue using Stacks`

### Linux / Docker

**主题：进程、端口与服务排查**

- `ps -ef`
- `top`
- `htop`（如果服务器有）
- `netstat -tunlp` 或 `ss -tunlp`
- `lsof -i`
- `kill` / `kill -9`
- `nohup`

**掌握标准**

- 能查某个 Java 进程是否启动
- 能查端口被谁占用
- 能让程序在后台运行并把日志输出到文件

### 前端补充

**主题：JavaScript 基础 1**

- 变量、数据类型
- 条件语句和循环
- 函数
- 数组和对象

---

## Day 5

### 项目推进

- 今日目标：
- 今日实际完成：
- 遇到的问题：
- 明日衔接点：

### 八股模块

**主题：Spring / SpringBoot 基础**

- 什么是 IoC
- 什么是 AOP
- `@Component`、`@Service`、`@Repository`、`@Controller`
- `@RestController` 和 `@Controller` 的区别
- `@Autowired` 的作用
- SpringBoot 为什么更方便

### LeetCode

- `83. Remove Duplicates from Sorted List`
- `203. Remove Linked List Elements`

### Linux / Docker

**主题：软件安装与环境变量**

- `apt` 或 `yum` 的基本使用
- 查看 Java 版本
- 配置环境变量
- 查看开放端口
- 检查防火墙基础状态
- 理解公网 IP、内网 IP、端口映射

**掌握标准**

- 能在服务器上安装基础工具
- 能检查 Java 环境是否正常
- 知道为什么服务启动了但外网访问不到

### 前端补充

**主题：JavaScript 基础 2**

- DOM 基础
- 事件绑定
- 获取和修改元素内容
- 表单输入获取

---

## Day 6

### 项目推进

- 今日目标：
- 今日实际完成：
- 遇到的问题：
- 明日衔接点：

### 八股模块

**主题：SpringBoot Web 开发**

- SpringBoot 请求流程
- Controller、Service、Mapper 分层
- 统一返回体的作用
- 全局异常处理的作用
- 分页查询的基本思路
- 配置文件 `application.yml` 的常见作用

### LeetCode

- `104. Maximum Depth of Binary Tree`
- `226. Invert Binary Tree`

### Linux / Docker

**主题：Docker 基础实操**

- 镜像和容器的区别
- `docker pull`
- `docker images`
- `docker ps -a`
- `docker run`
- `docker stop`
- `docker rm`
- `docker logs`
- `docker exec -it`

**掌握标准**

- 能拉取一个镜像并启动容器
- 能查看容器日志并进入容器
- 能说清楚 `镜像 -> 容器 -> 运行服务` 的关系

### 前端补充

**主题：前后端交互基础**

- 什么是请求和响应
- `GET` 和 `POST` 的区别
- JSON 基本格式
- 使用 `fetch` 或 `axios` 发请求的基本写法

---

## Day 7

### 项目推进

- 今日目标：
- 今日实际完成：
- 遇到的问题：
- 下周计划：

### 八股模块

**主题：项目八股与面试表达**

- 项目背景
- 技术栈
- 模块拆分
- 一次请求的完整流程
- 核心表设计
- 你的个人贡献
- 项目难点和解决方案

### LeetCode

- `94. Binary Tree Inorder Traversal`
- `145. Binary Tree Postorder Traversal`

### Linux / Docker

**主题：Docker 部署一遍 MySQL**

- 启动 MySQL 容器
- 理解端口映射
- 理解数据卷
- 查看容器日志
- 进入容器查看运行状态
- 停止并重启容器

**掌握标准**

- 能自己用 Docker 起一个 MySQL
- 能解释为什么数据要挂载卷
- 能说清楚容器删了为什么数据可能丢

### 前端补充

**主题：系统页面必备基础**

- 登录页表单结构
- 列表页和表格展示思路
- 新增/编辑弹窗的基本结构
- 给后端管理系统写一个最基础静态页面

---

## 每日晚复盘

- 今天项目推进是否达标：
- 今天八股是否完成：
- 今天 LeetCode 是否完成：
- 今天 Linux / Docker 是否完成：
- 今天前端补充是否完成：
- 今天最卡的点：
- 明天第一优先级：

---

## 本周验收标准

- 项目：至少有连续 7 天推进记录
- 八股：Java、MySQL、MyBatis、SpringBoot 主线过一轮
- LeetCode：完成 14 题，至少 8 题能复述思路
- Linux：能独立在服务器上完成日志查看、进程排查、权限处理、基础环境配置
- Docker：能独立启动并管理基础容器，至少完成 1 次 MySQL 容器实操
- 前端：知道 HTML / CSS / JS 最基本的页面搭建和接口联调方式
